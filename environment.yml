# Conda: RDKit + Flask (recommended on Windows; pip RDKit wheels are limited per Python version)
name: compound
channels:
  - conda-forge
dependencies:
  - python=3.11
  - rdkit
  - pip
  - pip:
    - flask>=3.0.0
